<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@4.4.1/dist/css/bootstrap.min.css" integrity="sha384-Vkoo8x4CGsO3+Hhxv8T/Q5PaXtkKtu6ug5TOeNV6gBiFeWPGFN9MuhOf23Q9Ifjh" crossorigin="anonymous">
    <link rel="stylesheet" href="/style.css">
    <title>{{title}}</title>
</head>

<body>
    <nav>
        <h4>Project Management Database</h4>
        <ul>
            <li><a href="/auth/login">Home</a></li>
            <li><a href="/secret">Logout</a></li>
        </ul>
    </nav>
    <div class="table-data">
        <div class="container mt-4">
            <h2>Student Information</h2>
            <form action="/auth/search" method="GET" class="mb-4">
                <div class="input-group">
                    <input type="text" name="usn" class="form-control" placeholder="Search by USN">
                    <input type="text" name="name" class="form-control" placeholder="Search by Name">
                    <div class="input-group-append">
                        <button type="submit" class="btn btn-primary">Search</button>
                    </div>
                </div>
            </form>

            {{#if userData.length}}
            <table class="table table-bordered">
                <thead style="background-color: #563d7c; color: rgb(244, 246, 248);">
                    <tr>
                        <th>USN</th>
                        <th>Name</th>
                        <th>Email</th>
                        <th>Phone Number</th>
                        <th>Project</th>
                        <th>Mentor Name</th>
                    </tr>
                </thead>
                <tbody>
                    {{#each userData}}
                    <tr>
                        <td>{{this.USN}}</td>
                        <td>{{this.Student_Name}}</td>
                        <td>{{this.Email}}</td>
                        <td>{{this.Phone_No}}</td>
                        <td>{{this.Project_Name}}</td>
                        <td>{{this.Name}}</td>
                    </tr>
                    {{/each}}
                </tbody>
            </table>
            {{else}}
            <p>No Data Found</p>
            {{/if}}
        </div>
    </div>
</body>

</html>
